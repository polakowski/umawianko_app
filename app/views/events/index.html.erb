<div class='fluid-content-wrapper p-m--t'>
  <h1 class='m-m--b deco-header'><span>Upcoming Events</span></h1>
  <%= render partial: 'events/new_event_button' %>
  <%- @events.map do |event| %>
    <%= render partial: 'event/card', locals: { event: event } %>
  <% end %>
</div>
